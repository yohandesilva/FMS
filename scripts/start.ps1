# Navigate to the server directory
cd server

# Install back-end dependencies (if not already installed)
#npm install

# Start the Node.js server
node index.js

# Print a success message
Write-Output "Server is running!"